# 企业名单工作台设计与真实链路验收

验收日期：2026-08-24
验收状态：工程验收通过，待用户人工签收

## 视觉基准

- 用户确认的名单详情参考图：`/var/folders/rz/d7pf51r95c726vt32184rgmh0000gn/T/codex-clipboard-ee533b93-b4ee-4f71-893b-fb25b22ab183.png`
- 实现截图：[名单详情（桌面端）](./design-qa/list-detail-desktop.png)
- 实现截图：[名单详情（移动端）](./design-qa/list-detail-mobile.png)
- 对照图：[参考图与实现并排对照](./design-qa/reference-comparison.png)
- 完整流程证据：[`design-qa/full-flow-2026-08-24`](./design-qa/full-flow-2026-08-24)
- 最终真实名单：[截图 41](./design-qa/full-flow-2026-08-24/41-final-real-list.jpg)

## 实现对照结论

- 保留参考图的深蓝顶部导航、居中主导航、名单标题与条件标签、状态摘要、白底数据表格、右上角导出与继续核验操作。
- 产品信息架构保持为“找企业 / 我的名单”两个主入口；配置使用抽屉承载，没有迁移为通用 Dashboard、Users、Settings 后台。
- 在参考图基础上补齐了全部名单入口、企业详情抽屉、来源冲突、单企业企查查核验、公开信息 HTML 报告与配置抽屉。
- 移动端采用横向可滚动表格与全宽详情抽屉；顶部导航、配置入口和账号入口无重叠。
- 路由切换现在会自动回到页面顶部，避免从长名单进入报告后直接落在报告中段。

## 真实数据验收

- 获客助手：真实检索生成 10 家上海企业名单，名单筛选条件、工商字段与脱敏联系方式正常展示。
- 企查查：对“上海上药生物医药有限公司”完成真实单企业核验；新来源事实独立保存，字段冲突可在详情抽屉查看，没有覆盖获客助手原始快照。
- Ego Lite：对同一企业完成官网、招聘网站和公开新闻检索，生成 18 条公开证据的 HTML 报告。
- 报告证据区分“匹配本企业 / 关联主体 / 行业背景”，搜索跳转链接明确标记，报告摘要优先采用精确匹配证据。
- 导出：CSV、XLSX、结构化 JSON 均从页面成功触发；注册资本单位固定为万元，经营状态中文化，联系方式保持脱敏。XLSX 又使用独立解析器回读，确认工作表、11 行、17 列、冻结首行和自动筛选范围均有效。

## 交互验收

- 找企业：最低条件不足时阻止调用；补齐关键词后显示预计调用量和二次确认，支持返回修改。
- 我的名单：总览搜索、进入名单、企业搜索、经营状态筛选、报告状态筛选正常。
- 企业详情：工商信息、来源与冲突、公开信息报告三个页签正常；企查查核验前明确提示单次调用及可能消耗积分。
- 配置抽屉：数据来源、整理规则、高级三个页签正常；四类数据源状态与测试入口可见。
- HTML 报告：查看、刷新和下载 HTML 正常；打印入口可见；移动端报告正文可独立滚动阅读。
- 全局搜索：可从顶部搜索企业或名单并跳转到匹配结果。
- 干净浏览器页重新打开真实名单后，控制台无错误日志。

## 自动化验证

- 全部单元/集成测试：555 通过，1 跳过（72 个测试文件）。
- 其中前端单元测试：169 通过，1 跳过；Provider Worker：25 通过；Supabase Functions：99 通过；Agent/Claude 工具链：262 通过。
- 数据库权限、初始化、租户隔离、来源连接、规则保存与 Web 证据：288 项 pgTAP 全部通过。
- 前端与 Worker 类型检查：通过。
- 前端生产构建与 Provider Worker 构建：通过。
- 两个 WorkBuddy Skill 使用官方校验脚本检查：均通过。
- WorkBuddy 最终包：`release/enterprise-list-workbench-workbuddy-0.1.0.zip`，ZIP 完整性检查通过，SHA-256 见 `release/SHA256SUMS`。
- 商用依赖清单：CycloneDX SBOM 共 321 个可追溯组件，许可证产物已生成且无发布阻塞项。
- 生产级 E2E 脚本已完成类型检查；按照当前浏览器验收约束，本轮通过应用内浏览器逐项执行主流程，没有另行启动 Playwright CLI。

## 已解决问题

- 修正注册资本原始“元”到界面“万元”的换算，避免金额放大一万倍。
- 修正企查查真实经营状态文本的归一化，并防止后续未知值覆盖已知状态。
- 修正来源快照未参与名单展示、核验完成后界面不自动更新的问题。
- 修正真实报告任务返回字段与前端企业详情入口不一致的问题。
- 修正移动端顶部导航拥挤、详情抽屉布局和跨路由滚动位置残留。
- 修正 Ego Lite 报告把集团或相似主体结果当成本企业证据的问题。
- 修正 ExcelJS 浏览器预构建包的依赖不可追溯与浏览器卡住问题；前端改为基于既有 MIT 许可 JSZip 生成标准 XLSX，Worker 继续使用可锁定版本的 ExcelJS。
- 修正密码重置邮件重复拼接回调路径、回调页无法进入 Hash Router 和密码确认中文校验问题。
- 修正并行验收环境共用 Vite 缓存导致懒加载页面失效的问题；每个环境现在使用独立缓存目录。
- 修正 JSON/CSV/HTML 后端导出的 MIME 类型与私有存储白名单不一致问题。
- 修正规则审阅中空值展开成原始 JSON、供应商名称和经营状态未中文化的问题。
- 修正名单移出操作缺少二次确认的问题；确认后只移出名单成员，不删除企业主记录或来源事实。
- 修正上游模板错误页暴露外部商业品牌和文档链接的问题。

## 控件覆盖

- 桌面端：找企业、我的名单、名单详情、企业详情、公开报告、全局搜索、配置抽屉、来源连接、字段映射、规则、运行、导出/API、冲突、审阅、批次、成员与审计均已点击验证。
- 移动端：找企业、名单总览、名单详情横向表格、企业详情抽屉、配置抽屉、公开报告均已检查。
- 数据源测试：获客助手、企查查、文件导入、Ego Lite 四个入口均达到“测试完成”状态；可能计费的真实调用保留二次确认。
- “移出企业”已在隔离环境同时验证取消与确认：取消后仍为 3 家，确认后名单成员变为 2 家，企业主记录和来源事实完整保留。

## 设计健康度（工程验收）

1. 信息架构：9/10。主导航保持“找企业 / 我的名单”两入口，高级能力收进配置。
2. 核心任务闭环：9/10。真实找企、核验、报告、导出均可到达，且来源与调用限制可见。
3. 桌面视觉一致性：9/10。与确认参考图保持同一深蓝导航、白底表格和信息密度。
4. 移动可用性：8/10。主要流程完整可用；宽数据表采用明确提示的横向滑动模式。
5. 可访问性与中文化：9/10。交互控件已补中文名称，关闭、通知、筛选和复制等入口可识别。
6. 发布可信度：9/10。测试、构建、SBOM、许可证、真实数据和截图证据齐全；最终 1 分留给用户人工签收与目标部署环境验收。

## 非阻塞事项

- 生产构建仍提示基础主包较大；业务页面和导出能力已经按需加载，不影响当前功能，可在后续发布优化阶段继续拆分底层框架依赖。
- Browserslist 数据提示可更新；不影响本次验收。
