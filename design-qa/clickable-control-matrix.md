# 可点击控件验收矩阵

验收日期：2026-08-28
环境：主真实数据环境 `5175`；隔离破坏性测试环境 `5176`；全新数据库安全测试环境 `54542`。
原则：同一类重复行按钮按控件类型验收一次；真实计费操作只在确认框内明确调用量后执行；删除类操作同时验证取消与确认。

| 页面 / 状态 | 已点击控件 | 期望与结果 | 证据 |
|---|---|---|---|
| 本地首次使用 | 访问密码、再次输入、设置并进入 | 只显示两个密码框；不要求邮箱、注册或初始化码；内部身份和默认工作空间自动创建 | 全新隔离数据库；Playwright CLI |
| 本地解锁 | 本机菜单、锁定工作台、访问密码、进入工作台 | 锁定后只显示一个访问密码框；正确密码直接回到“找企业”，控制台 0 错误 | 全新隔离数据库；Playwright CLI |
| 多用户首次管理员 | 初始化码、姓名、邮箱、密码、创建管理员 | 仅在多用户部署模式使用；创建后进入工作空间初始化，一次性入口随后关闭 | 独立 Playwright 验收 |
| 多用户工作空间初始化 | 名称、Slug、创建并初始化 | 合法值一次创建工作空间、四类来源和默认规则 | 独立 Playwright 验收；pgTAP |
| 找回密码 | 忘记密码、发送邮件、回调、新密码、确认密码、保存 | 邮件品牌正确；回调进入应用；两次密码不一致被阻止；有效密码更新成功 | 浏览器与 Mailpit 记录 |
| 顶部导航 | Logo、找企业、我的名单、全局搜索、配置、账号菜单 | 均可达；路由切换回到顶部；移动端无重叠 | `14`、`28`–`33` |
| 找企业 | 关键词、地区、行业、数量、状态、联系方式、资质、资本、参保、日期、企业类型、风险项 | 目录选择值有效；不足两类范围条件禁止执行；完整条件进入调用确认 | `01`–`03` |
| 调用确认 | 返回修改、确认开始 | 展示完整条件、目标数量、预计分页调用次数及交集规则；确认后才提交 | `03`；真实获客助手任务 |
| 我的名单 | 搜索、新建名单、名单卡片、空结果恢复 | 搜索/空态/进入详情/返回找企业正常 | `04`、`05` |
| 名单详情 | 企业搜索、经营状态、报告状态、行、分页、每页数量 | 筛选和分页正常；企业行打开详情；移动端表格可横向滑动 | `06`、`30` |
| 企业详情 | 企业信息、来源与冲突、公开信息报告三个页签 | 工商字段、单位、中文状态、来源差异和报告状态均正常 | `08`、`09`、`11`、`31` |
| 企查查核验 | 批量核验、单企业核验、取消、确认 | 明确企业数与调用次数；真实单企业核验完成；原来源不被覆盖 | `07`、`10`；真实 QCC 记录 |
| 名单选择操作 | 全选、单选、移出企业、取消、确认 | 取消保留 3 家；确认后名单成员变 2 家，企业主记录和事实保留 | `39`；隔离数据库记录 |
| 前端快速导出 | CSV、Excel、JSON | 三种格式均可生成；Excel 回读为 11 行、17 列、冻结首行并带筛选 | 浏览器下载与解析记录 |
| 后端导出 | 新建导出、范围、名单、规则运行、格式、字段、取消、提交、下载 | JSON 任务完成 2 行；私有对象可签名下载并回读；错误任务不可下载 | `23`；存储回读记录 |
| API / MCP | 复制 API 基址、OpenAPI、企业示例、MCP、任务入口 | 复制控件可用；界面只给受控领域接口并提示令牌边界 | `23`；浏览器记录 |
| 配置抽屉 | 数据来源、整理规则、智能助手、高级四个页签及深层链接 | 抽屉作为单一“配置”入口；可复制 MCP 连接地址，并跳转来源、映射、规则、任务、导出、审阅、审计 | `15`–`17`、`32` |
| 数据来源 | 编辑、保存、测试连接、启停 | 四类来源状态真实；获客助手/QCC/Ego Lite 不在浏览器保存密钥；文件上传可用 | `18`、`19` |
| 文件导入 | 上传入口、文件选择、映射方案、预览、取消、确认导入 | 真实获客助手 CSV 3 行/8 字段预览；后端接收 3、成功 3、失败 0 | `26`、`37`、`38` |
| 字段映射 | 新建、方案字段、单位、添加字段、选择统一字段、删除字段、保存版本 | 新建“隔离验收文件映射”v1 成功；供应商内置映射保持只读 | `20`；隔离浏览器与数据库记录 |
| 规则模板 | 新建、条件、条件组、AND/OR、删除、加分规则、风险门禁、启停、删除规则 | 所有编辑控件逐项点击；删除后回到空规则状态；Schema 校验正常 | `21`；隔离浏览器记录 |
| 规则保存与试算 | 保存新版本、真实批次、10 家试算、执行任务链接 | “隔离验收制造业规则”v1 保存；真实批次试算完成且结果进入审阅 | 隔离数据库记录 |
| 执行任务 | 刷新、标签、整批执行、取消、确认 | 未满足条件时按钮禁用；确认框说明不可变批次和规则版本 | `22` |
| 企业审阅 | 规则任务、结论筛选、搜索、查看证据、Web 证据、人工决定、说明、关闭、保存 | 字段值与供应商中文化；人工结论独立保存，不覆盖原事实 | `25`；隔离浏览器与数据库记录 |
| 来源冲突 | 来源/状态筛选、企业链接、审阅链接 | 可定位字段级差异并进入企业/审阅上下文 | `24` |
| 设置与审计 | 数据源优先级上移/下移、保存、成员/工作空间/审计入口 | 优先级通过受控 RPC 保存；成员与审计记录按工作空间隔离 | `27`；pgTAP |
| HTML 报告 | 返回名单、刷新状态、打印、下载 HTML、证据链接 | 真实 Ego Lite 18 条证据；报告可刷新、打印、下载；来源链接独立打开 | `12`、`13`、`33` |
| 错误恢复 | 重新加载、返回上一页 | 中文品牌错误页替代上游模板说明；两条恢复路径均保留 | 源码与构建检查 |

截图编号均位于 `design-qa/full-flow-2026-08-24/`。浏览器记录来自 Codex 应用内浏览器的实际 DOM、页面状态、文件选择与下载验证，不使用模拟后端。
