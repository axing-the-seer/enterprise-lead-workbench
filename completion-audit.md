# 生产交付完成度审计

| 明确需求 | 生产实现 | 验收证据 |
|---|---|---|
| 不是 Demo，而是完整软件 | 保留 Vite + React Router + ra-core + Supabase + Provider Worker；认证、租户、任务、审计、存储、导出均为真实链路 | `npm run build`；74 个测试文件、564 项通过、1 项跳过；12 项 Deno 合同/报告测试通过 |
| 主界面只保留“找企业 / 我的名单” | 顶部只显示两个主入口；其他能力统一进入“配置”抽屉 | 截图 `01`、`04`、`15` |
| Agent/Skill 完成前置找企，用户进入 Web UI 管理 | Skill 收敛条件、说明调用量、提交任务并返回名单 ID/路由；Web UI 读取同一租户数据 | 两个 `SKILL.md` 与 `app-contract.md` |
| 获客助手字段必须真实有效 | 地区、行业、资质目录及资本、参保、日期、企业类型、联系方式、风险等条件与 KC 合同对齐 | `kc-query-contract.md`；真实 10 家名单 |
| 企查查用于企业核验 | 仅对已入库企业调用基本工商详情，保留字段来源与冲突，不冒充名单搜索或其他未接入能力 | Deepseek-V4-Flash 在 WorkBuddy 中对阿里巴巴真实调用 1 次 `company_detail`；1/1 接收，新增 18 个当前企查查事实，未新建名单 |
| Ego Lite 单企业 HTML 报告 | 检索官网/业务、招聘、新闻；Agent 只负责整理证据，生成自包含、可打印、可下载、可追溯报告；用户正文禁止出现证据主键、字段名和内部枚举 | 阿里巴巴与蚂蚁集团各发现 24 条真实公开证据；阿里巴巴报告第 2 版已通过 Chrome 检查，正文不含 `ev-*`、`USCC`、`broad_context` 等内部标记 |
| 每个企业有详情和报告入口 | 名单行打开详情侧栏；详情含报告页签；报告路由 `/#/reports/{jobId}` | 截图 `08`–`13` |
| 上传与 Web 证据真正可用 | 文件选择器、真实预览、后端导入已贯通；Web 证据由 Ego Lite 写回证据表 | 截图 `37`、`38`；真实报告记录 |
| 行业规则可配置 | RuleTemplate v1 支持条件树、缺失策略、P1/P2、加分、风险门禁、版本化和真实批次试算 | 隔离规则 v1；规则控件点击记录 |
| 不同数据商映射规则清楚 | 供应商内置映射只读；文件映射可版本化；原始快照、规范化事实、冲突与优先级分层保存 | 映射页面、`provider-mapping.mdx`、`source-policy.md` |
| 中国实际交付场景 | Excel/CSV/JSON/HTML，受控 REST/MCP，可被 WorkBuddy 与其他 Agent 读取 | 导出回读、OpenAPI/MCP 页面、Skill 包 |
| 两个可安装 Skill | `enterprise-lead-workbench` 与 `enterprise-public-report` 独立安装、共用工作台 MCP | 两个测试 ZIP 均以根目录 `SKILL.md` 开始；找企业包含 5 个文件，报告包含 4 个文件与 `save_report.py`；压缩完整性和官方 `quick_validate.py` 校验均通过 |
| Apple 风格但不照搬通用后台 | 深蓝导航、克制留白、柔和灰底、圆角卡片、清晰层级；参考图的名单密度与交互结构被保留 | 桌面/移动截图及 `reference-comparison.png` |
| 所有可点击控件验收 | 控件按唯一交互类型逐项检查；破坏性操作在隔离环境验证取消和确认 | `design-qa/clickable-control-matrix.md` |
| 商用风险可审计 | 上游 MIT 许可与运行依赖生成 CycloneDX SBOM 和第三方许可；根项目与文档生产依赖 0 漏洞 | `compliance/`、`THIRD_PARTY_NOTICES.md`；321 个组件、33 份生产 source map/服务端运行依赖复核通过 |

## 本轮真实链路验收

- WorkBuddy 模型：`Deepseek-V4-Flash`。
- 模糊需求引导：用 `/enterprise-lead-workbench` 锁定测试后，Agent 先把“工业机器人集成和设备维保、杭州市场”转为四类待确认客群，只追问优先客群和首批数量，并用“等待你的回答”交付块暂停；没有调用获客助手、没有新增名单。为避免 WorkBuddy 在存在同类 Skill 时自动路由到原始获客助手，确定性测试统一使用 `/` 锁定编排 Skill。
- 获客助手：创建原名单“杭州软件信息服务企业精选”，10 家均为杭州企业；Excel 已下载并回读为 11 行（1 行表头 + 10 行数据）。
- 企查查：任务 `4f277e9f-e201-46cc-8abf-4ef6656f4f45` 完成，`received=1`、`accepted=1`、`rejected=0`；结果仅返回 `verified_company_ids`，不再返回或创建 `company_list_id`。
- 原名单：`599921df-eaaa-408e-8250-e1608aa64326`，核验前后均为 10 家；名单总数核验前后均为 4，历史遗留“数据批次”名单仍为 1，没有新增。
- Chrome：无需重新登录即可打开 `/#/lists/599921df-eaaa-408e-8250-e1608aa64326`；名单标题、10 家分页、阿里巴巴已更新工商字段及企业详情侧栏均可见。
- 企业报告：证据任务 `8dc53d0a-6526-44f7-97e6-5577355c98ed` 当前报告为第 2 版（报告 ID `71af40ec-efc4-445c-9d5e-8e2a78753047`），第 1 版保留为已替代记录；38 个用户可见文本字段及最终 HTML 的内部标记扫描结果均为 0，引用锚点使用 `source-1` 这类普通序号。
- MCP：WorkBuddy 完成 OAuth 重连后显示 20/20 工具可用；Agent 实际调用工作空间、数据源、企查查任务和轮询链路成功。
- 测试 Skill ZIP 由 `npm run build:test-skills` 可重复生成；构建会校验所有必需引用文件，避免再次产出“能识别但运行时缺文件”的不完整压缩包。

生产边界：供应商账号额度、企查查具体套餐权限、获客助手服务可用性以及 Ego Lite 能否访问某个公开站点属于外部状态；系统会把未授权、站点限制、验证码和部分覆盖显式记录，不会把“未取得”写成“不存在”。
